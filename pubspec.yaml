name: pure_wasm_runtime
description: Experimental pure Dart WebAssembly runtime (MVP interpreter).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3

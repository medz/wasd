include: package:lints/recommended.yaml

analyzer:
  exclude:
    - old.*/**
    - tool/**
    - example/**

linter:
  rules:
    - public_member_api_docs
    - prefer_final_fields
    - prefer_final_locals
